package commands;

import utils.Writer;

import javax.persistence.EntityManager;
import java.io.BufferedReader;

public class ListMyBooks implements Command {

    public void execute(EntityManager entityManager, BufferedReader reader, Writer writer) {

    }
}
